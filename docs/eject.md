# Eject Setup Docs

After your project is set up with the language and infrastructure you need,
remove this docs/ directory:

```bash
just eject
```

This deletes the entire `docs/` directory. Your justfile, lefthook.yml, and
other config files are already configured and will continue to work.

You can also remove the `eject` recipe from `justfile` afterward.
